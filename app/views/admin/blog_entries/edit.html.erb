<h1>Editing blog entry</h1>

<% form_for([:admin, @blog_entry]) do |f| %>
  <%= render :partial => f,
             :locals  => { :button_name => "Update" } %>
<% end %>

<%= link_to 'Back', admin_blog_entries_path %>
